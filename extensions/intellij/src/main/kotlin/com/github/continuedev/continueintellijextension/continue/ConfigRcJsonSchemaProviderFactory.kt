// Intentionally disabled for IntelliJ 2026.2+ compatibility.
// The com.jetbrains.jsonSchema.extension API (JsonSchemaProviderFactory,
// JsonSchemaFileProvider, SchemaType) was removed / restructured in recent
// IntelliJ platform releases, and the optional JSON plugin no longer exposes
// this extension point. JSON schema autocomplete for .continuerc.json is
// therefore not available in this fork; the rest of Continue works unchanged.
//
// Original implementation archived in git history (continue/ConfigRcJsonSchemaProviderFactory.kt @ 1.0.68).
