// Intentionally disabled for IntelliJ 2026.2+ compatibility.
// The com.jetbrains.jsonSchema.extension API (JsonSchemaProviderFactory,
// JsonSchemaFileProvider, SchemaType) was removed / restructured in recent
// IntelliJ platform releases, and the optional JSON plugin no longer exposes
// this extension point. JSON schema autocomplete for config.json is therefore
// not available in this fork; the plugin still bundles the schema file and
// the rest of Continue works unchanged.
//
// Original implementation archived in git history (continue/ConfigJsonSchemaProviderFactory.kt @ 1.0.68).
